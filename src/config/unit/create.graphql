mutation CreateUnit($input: CreateUnitInput!) {
  createUnit(input: $input) {
    clientMutationId
    entity: unit {
      id
      name
      abbreviation
      baseUnit
      multiplier
      trackersList {
        id
        entriesList {
          id
        }
      }
    }
  }
}
